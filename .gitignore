# Python
__pycache__/
*.pyc

# Local virtual environments
rego_env/
.venv/
venv/

# Vendored third-party libraries (cloned separately — see README)
vendor/

# Generated simulation outputs, checkpoints, logs
post/
outputs/
*.npz
*.vtk
*.vtu
*.pvd
*.pkl
log.liggghts
physics_summary.txt
restart_forced_*.data

# Ansys Rocky scratch/cache directories
**/*.rocky.files/tmp/
**/*.rocky.files/simulation/

# Archived/superseded diagnostic data and ad-hoc analysis run output
# (scripts in simulation/analysis/*.py and findings in *.md are tracked;
# their raw per-run logs/json dumps are regenerable scratch, kept locally
# in runs/ for reference but not committed)
simulation/old_results/
simulation/analysis/runs/

# Generated plots/images (tracked doc assets are excluded below)
*.png
!docs/**/*.png
